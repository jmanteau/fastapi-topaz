# Main application environment variables
# This file is merged with env.authentik to create .env on first run
# OIDC credentials are auto-synced by 'make tf-apply'

# Authentik OIDC (auto-populated by 'make tf-apply' or 'make sync-oidc-secret')
# OIDC_CLIENT_ID=webapp-client
# OIDC_CLIENT_SECRET=<auto-synced from terraform>

# Topaz (usually no credentials needed for local dev)
TOPAZ_TENANT_ID=
TOPAZ_API_KEY=

# Application
SECRET_KEY=change-me-in-production
DEBUG=false
